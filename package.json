{
  "name": "ekcode",
  "version": "1.0.0",
  "private": true,
  "description": "EkCode - One Nation, One Material Code (Full-Stack Monorepo)",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm --prefix frontend install --legacy-peer-deps && npm --prefix frontend run build",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.0",
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.4",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  }
}
